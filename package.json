{
  "name": "codestreak",
  "version": "1.0.0",
  "description": "CodeStreak is a webapp that motivates you to push to github",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint . && npx prettier --check .",
    "test": "echo \"Error: no test specified\"",
    "dev": "cd packages/backend && nodemon backend.js",
    "start": "cd packages/backend && node backend.js"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "prettier": "^3.0.3"
  }
}
